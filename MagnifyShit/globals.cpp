#include "framework.h"

float g_zoom = 1.0f;                // Initial zoom level
HWND g_hMainWnd = nullptr;          // Handle to the main window (initialized as nullptr)
bool g_showFooter = true;           // Flag for showing footer
const int FOOTER_HEIGHT = 30;       // Define the footer height
